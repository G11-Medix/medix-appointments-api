meta {
  name: [IPS-MOCK] Patch Cancelar Cita Assistant
  type: http
  seq: 18
}

patch {
  url: {{API_URL}}{{API_PREFIX}}/citas/{{ID_CITA}}/cancelar
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

docs {
  [IPS-MOCK] Este endpoint cancela la cita a través de la IPS mock configurada para la institución.
}

body:json {
  {
    "id_institucion": {{ID_INSTITUCION}},
    "motivo": "{{MOTIVO_CANCELACION}}"
  }
}

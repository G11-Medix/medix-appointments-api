meta {
  name: [IPS-MOCK] Get Confirmacion Cita IPS
  type: http
  seq: 12
}

get {
  url: {{API_URL}}{{API_PREFIX}}/instituciones/{{ID_INSTITUCION}}/citas/{{ID_CITA}}/confirmacion
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

docs {
  [IPS-MOCK] Este endpoint devuelve la información de confirmación de una cita en la IPS mock configurada para la institución.
}

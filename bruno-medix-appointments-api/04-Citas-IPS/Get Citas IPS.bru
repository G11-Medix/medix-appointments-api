meta {
  name: [IPS-MOCK] Get Citas IPS
  type: http
  seq: 8
}

get {
  url: {{API_URL}}{{API_PREFIX}}/instituciones/{{ID_INSTITUCION}}/citas/?id_paciente={{ID_PACIENTE_IPS}}&desde={{FECHA_DESDE}}&hasta={{FECHA_HASTA}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

docs {
  [IPS-MOCK] Este endpoint lista citas desde la IPS mock configurada para la institución.
}
